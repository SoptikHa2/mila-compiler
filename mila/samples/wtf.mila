program wtf;

begin
    setupSharedInts(10);
    setSharedInt(0, 42);
    :a;

    comeFrom a;
        writeln(getSharedInt(0));
    exit;


    comeFrom a;
        setSharedInt(0, 26);
        writeln(getSharedInt(0));
    exit;
end.
